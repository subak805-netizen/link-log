# LINK.LOG — 디자인 가이드

> UI/디자인 작업 전 이 문서를 먼저 읽고 따른다. 디자인이 바뀌면 이 문서를 무조건 업데이트한다.

## 컨셉
**FIT.LOG · EAT.LOG와 완전히 같은 색·뼈대.** (사용자 결정 2026-08-17)
- 모노스페이스 폰트, 각진 박스(radius 0), 굵은 검은 선(2px 바깥 / 1px 안쪽), 숫자는 Doto 픽셀 폰트.
- 배경 흰색, 베이지는 패널·칩 배경에만.
- **이모지 사용 금지.** 텍스트 라벨 또는 SVG만.

## 컬러 토큰 (FIT.LOG와 동일)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--paper` | `#FFFFFF` | 배경·카드 |
| `--panel` | `#FBF8F3` | 옅은 베이지 패널·칩 배경 |
| `--dim` | `#F2EBE0` | 빈 칸 |
| `--beige` | `#E3D8C6` | 옅은 구분선 |
| `--ink` | `#181613` | 본문·테두리·활성 배경 |
| `--mut` | `#8B8377` | 보조 텍스트·라벨 |
| `--olive` | `#5F6B33` | 일 / 살아있음 |
| `--terra` | `#C25E3A` | 죽은 링크 / 로고 점 |
| `--gold` | `#EBC63C` | 취향 / 포커스 / 주의 |

## 타이포
- 본문 = 모노스페이스, 13px / 숫자·로고 = `Doto` 900
- 라벨 = 9~10px, `letter-spacing .13~.16em`, `--mut`
- 섹션 제목 = 한글 14px 800 + 영문 캡션 9px 병기 (예: `전체  ALL`)

## 형태
- radius **0**, 그림자 없음. 카드 바깥선 2px, 내부 1px, 전부 `--ink`
- 인풋·셀렉트 `appearance:none` 필수(iOS Safari 대응)
- 포커스 = `box-shadow:inset 0 0 0 2px var(--gold)`

## 탭 구조 (4개)
홈(HOME) · 전체(ALL) · 브랜드(BRAND) · 정리(CLEAN)
- 폰 = 하단 고정 탭바 / PC(≥960) = 왼쪽 사이드바 236px
- 탭을 늘리지 않는다. 하위 구분은 세그·칩으로 (EAT.LOG 원칙과 동일)

## 분류 = 폴더 아님, 태그 2축
폴더가 엉망이던 이유 = 한 링크가 여러 폴더에 속해야 해서. 그래서 축을 둘로 나눈다.
- **용도축(use)**: `일` `취향` `도구` `참고` — 하나만 고름
- **분야축(field)**: `원부자재` `공장` `브랜드` `폰트` `이미지` `문서` `금융` `기타` — 하나만 고름
- 예: 29CM = `취향`+`브랜드` / 홈택스 = `일`+`금융` / 동대문종합시장 = `일`+`원부자재`

## 중복 판정 (필독)
크롬·사파리·노션에 **같은 사이트가 중복 저장**돼 있는 것이 이 앱의 핵심 문제.
`normUrl()` 하나로만 판정한다 — 다른 곳에서 URL 비교를 직접 하지 말 것.
- `www.` `m.` `mobile.` 접두어 제거
- 끝 `/` 제거, 소문자화
- `utm_*` `fbclid` `gclid` 등 추적 파라미터 제거
- 노션 CSV를 가져올 때 기존 링크와 같으면 **새로 만들지 않고 브랜드 정보(속성·국가·금액대)만 채운다** — 이게 크롬↔노션 중복을 없애는 지점

## 컴포넌트
- **링크 행(.lrow)**: 파비콘 + 제목 + 도메인 + 태그 + 열기. 카드 그리드 아님(링크는 사진이 없어 행이 밀도가 높다).
- **파비콘**: `google.com/s2/favicons`. 실패하면 `onerror`로 글자 아바타(.fico.txt) 대체 — 깨진 이미지 아이콘 금지.
- **잠자는 링크**: 저장한 지 오래됐고 `opens===0`인 것 3개. 홈 최상단. "쌓아만 두고 안 본다"를 고치는 장치라 절대 빼지 않는다.
  **`use==='도구'`는 제외한다** — Gmail·지도처럼 습관으로 여는 것이 올라오면 이 칸이 무의미해진다(실제로 첫 실행에서 그렇게 나왔음).
- **출처 배지(.src)**: 크롬/사파리/노션. 어디서 왔는지 항상 보이게 — 합치기 신뢰의 근거.
- **죽은 링크**: `dead:true`면 행 `opacity:.5` + `terra` 배지. **지우지 않는다**(사용자가 직접 판단).

## 반응형
**< 960px (폰)** — `max-width:430px` 중앙, 1열, 하단 탭바, 상세는 바텀시트
**≥ 960px (PC)** — 사이드바 236px, 2열(≥1320px 3열), 상세는 중앙 모달 720px
- 검증 필수 폭: 320 / 375 / 768 / 960 / 1280 / 1920

## 가로로 늘어나지 않게 (필수)
- 가로 스크롤 금지. 칩·필터는 `flex-wrap:wrap`.
- **그리드는 항상 `minmax(0,1fr)`** (`1fr`만 쓰면 긴 URL이 카드를 화면 밖으로 밀어냄 — FIT.LOG 실제 버그)
- URL 표시는 `.mono.ell`로 한 줄 말줄임. 본문 텍스트는 `overflow-wrap:anywhere`.

## 원칙
- 이 앱은 링크 **보관함이 아니라 다시 꺼내 쓰는 도구**. 저장보다 "여는 것"이 주인공.
- 그래서 모든 행에 열기 버튼이 있고, 열면 `opens`가 올라가고, 그 숫자가 홈 정렬을 만든다.
