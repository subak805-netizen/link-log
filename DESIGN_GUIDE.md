# LINK.LOG — 디자인 가이드 (v2 · 단순판)

> UI/디자인 작업 전 이 문서를 먼저 읽고 따른다. 디자인이 바뀌면 이 문서를 무조건 업데이트한다.

## 이 앱의 한 문장
**흩어진 즐겨찾기를 하나로 모아, 사파리 즐겨찾기 창처럼 빨리 꺼내 쓰는 도구.**
2026-08-18 사용자 피드백("너무 복잡하게 했어")으로 전면 단순화. 기능을 더하기 전에 이 문장과 대조할 것.

## 화면 = 사실상 1개
- **홈**: 빠른입력 한 줄 + 검색 + 폴더 트리. 이게 전부다.
- **설정**(시트): 로그인 · 파일 넣기(합치기/지우고 이 파일로) · 백업 · 전부 지우기.
- 탭바 없음. 브랜드/관리/정리 탭, 통계, 잠자는 링크, 자주 여는 것, 스타일 태그, 자동분류 화면 → **전부 폐지됨. 부활 금지.**

## 분류 = 2단계 (대분류 → 폴더)
- 씨앗: 브랜드(아동·유아/키즈·주니어/강아지옷/홈웨어/수영복/데일리/디자이너/빈티지·프렌치/가방·신발/주얼리/코스메틱) ·
  생산(원단/부자재·라벨/공장·봉제/니트·편직/프린팅·자수/도매시장/외주·견적) ·
  자료(매거진/블로그/글감/로고·그래픽/폰트/이미지) ·
  쇼핑(편집샵 국내·해외/플랫폼·몰/직구몰/스마트스토어) ·
  도구(문서/AI·검색/마케팅/배송·물류/세무·행정/내 앱) · 살림(인테리어/요리/물생활/건강/캠핑) · **미분류**
- `BASE`는 순서용일 뿐. 폴더·대분류는 **사용자가 ＋로 만들고**(빈 폴더는 `S.folders`), 이름도 직접 바꾼다.
- **앱은 절대 폴더를 추정해 배정하지 않는다.** 새 링크·가져온 링크는 전부 **미분류**로. 사용자가 끌어다 놓는다.
  (과거 사고: 판정 실패분을 '아웃도어·데일리'에 800개 처박음 — 그 반성이 이 규칙이다)
- 옛 체계 데이터(01_브랜드…/use·field/기타/분류전)는 `OLDG`/`OLDF`로 이관, 모르면 미분류.

## 조작 = 사파리 방식
- 제목 클릭 → 바로 열림(새 탭). 수정은 행 오른쪽 「…」만.
- 링크·폴더는 **드래그앤드롭**으로 이동(`data-dragl`/`data-dragf` → `data-drop`).
- 폴더 만들기 = 대분류 오른쪽 ＋ / 이름 바꾸기 = 「이름」 버튼. prompt 기반, 시트 없음.

## 지켜야 할 것
- 토큰·모노스페이스·radius 0·이모지 금지 — FIT.LOG와 동일.
- 중복 판정은 `normUrl()` 하나로만.
- 「지우고 이 파일로」도 `isMine()`(직접 추가·메모·열어본 것)은 지우지 않는다.
- 개인 링크는 저장소 금지 — SEED는 배포본에서 항상 `[]`, 데이터는 다운로드 폴더 JSON으로.
- 동기화: fpf 프로젝트 재활용, 문서 `users/{uid}/data/linklog`, S 통째 저장(links+folders).
- 검증 폭 375·1280, 가로 넘침 0.
